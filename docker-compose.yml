services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: relaycode
      POSTGRES_PASSWORD: relaycode
      POSTGRES_DB: relaycode
    ports:
      - "5432:5432"
    volumes:
      - relaycode-postgres:/var/lib/postgresql/data

volumes:
  relaycode-postgres:
