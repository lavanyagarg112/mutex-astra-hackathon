{
  "name": "relaycode",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "concurrently -n web,server -c blue,green \"npm run dev -w @relaycode/web\" \"npm run dev -w @relaycode/server\"",
    "dev:all": "concurrently -n web,server,daemon -c blue,green,magenta \"npm run dev -w @relaycode/web\" \"npm run dev -w @relaycode/server\" \"npm run dev -w @relaycode/daemon\"",
    "build": "npm run build -ws --if-present",
    "typecheck": "npm run typecheck -ws --if-present",
    "test": "npm run test -ws --if-present",
    "db:generate": "npm run db:generate -w @relaycode/server",
    "db:migrate": "npm run db:migrate -w @relaycode/server",
    "db:seed": "npm run db:seed -w @relaycode/server",
    "setup": "npm install && npm run db:generate"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.7.3"
  },
  "engines": { "node": ">=20" }
}
